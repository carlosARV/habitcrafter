<div class="row">
    {{#each routines}}
        <div class="col-md-3">
            <div class="card">
                <div class="card-body">
                    <h4
                        class="card-title d-flex justify-content-between align-items-center">{{title}}
                        <a href="/routines/edit/{{_id}}"><i
                                class="fa-solid fa-pencil"></i></a></h4>
                    <p>{{description}}</p>
                    <p>Reminder: {{formatDate date}}</p>
                    <!-- Aquí estamos llamando a un helper -->
                    <p>At: {{time}}</p>
                    <form action="/routines/delete/{{_id}}?_method=DELETE"
                        method="post">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit"
                            class="btn btn-danger btn-block btn-sm">
                            Delete
                        </button>
                    </form>
                </div>
            </div>
        </div>
    {{/each}}
</div>
